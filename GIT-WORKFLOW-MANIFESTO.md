# مانیفست مدیریت گیت پروژه

> این سند بر اساس بررسی تاریخچه‌ی واقعی commitها و برنچ‌های پروژه نوشته شده — نه یک استاندارد تئوریک عمومی. هر قانون اینجا، یا از یک نیاز واقعی تیم اومده، یا از یک اشتباهی که قبلاً رخ داده و نباید تکرار بشه. هر عضو تیم (فعلی یا جدید) موظف به رعایت این قوانینه.

**آخرین به‌روزرسانی:** این یک سند زنده‌ست؛ هر وقت روش کار تیم تغییر کرد، این فایل هم باید آپدیت بشه — نه اینکه واقعیت از سند جلو بیفته.

---

## ۱. سلسله‌مراتب برنچ‌ها

پروژه چهار سطح برنچ داره که هر کدوم نقش مشخصی دارن:

```
main
 └── develop
       └── sprint/N-<توضیح>
             └── feature/sX.Y-<توضیح>
             └── fix/sX.Y-<توضیح>
```

| سطح | نقش | چه زمانی آپدیت می‌شه |
|---|---|---|
| `main` | نسخه‌ی نهایی و پایدار | فقط در نقاط عطف رسمی (پایان دمو، ریلیز) |
| `develop` | یکپارچه‌سازی کلی تیم | وقتی یک sprint کامل تموم و تأیید شده |
| `sprint/N-*` | یکپارچه‌سازی کارهای یک اسپرینت | با merge شدن هر feature/fix داخلش |
| `feature/*`, `fix/*` | کار روزمره‌ی یک نفر روی یک تسک مشخص | هر commit روزانه |

هیچ‌کس مستقیم روی `main` یا `develop` کد نمی‌نویسه. کار همیشه از یک `feature/*` یا `fix/*` شروع می‌شه.

---

## ۲. قانون طلایی: ترتیب ساخت برنچ اسپرینت

**قبل از شروع اولین feature هر اسپرینت جدید، باید خود برنچ `sprint/N` از روی `develop` ساخته بشه.**

❌ اتفاقی که قبلاً افتاد و نباید تکرار بشه: دو تسک (`s1.1`, `s1.2`) که منطقاً باید بخشی از اسپرینت ۱ باشن، چون `sprint/1` هنوز ساخته نشده بود، از روی `sprint/0` منشعب و دوباره به همون merge شدن. `sprint/1` واقعی فقط بعداً و از نقطه‌ای دیرهنگام ساخته شد — که تاریخچه رو گیج‌کننده می‌کنه.

✅ ترتیب درست:
1. تصمیم بگیرید اسپرینت جدید شروع می‌شه.
2. بلافاصله `sprint/N-<توضیح>` رو از روی `develop` بسازید (حتی قبل از تعریف دقیق تسک‌ها).
3. بعد هر `feature/*` یا `fix/*` رو از روی همین `sprint/N` بسازید.

---

## ۳. قرارداد نام‌گذاری برنچ‌ها

```
sprint/<شماره>-<توضیح-کوتاه-با-خط-تیره>       مثال: sprint/2-embedding-model
feature/s<اسپرینت>.<شماره>-<توضیح>            مثال: feature/s2.1-model-design
fix/s<اسپرینت>.<شماره>-<توضیح>                 مثال: fix/s2.1-null-input-crash
```

اسم باید دقیقاً بگه این برنچ چیکار می‌کنه — بدون نیاز به باز کردن کد.

---

## ۴. قرارداد پیام Commit (Conventional Commits)

```
<type>(<scope>): <توضیح کوتاه>
```

| type | برای چی |
|---|---|
| `feat` | قابلیت جدید |
| `fix` | رفع باگ |
| `docs` | مستندات |
| `chore` | کارهای جانبی (dependency، تنظیمات) |
| `test` | اضافه/تغییر تست |
| `refactor` | بازنویسی بدون تغییر رفتار |

مثال: `feat(s2.1): add embedding model architecture`

---

## ۵. استراتژی Merge — سه سطح، سه قانون متفاوت

### سطح ۱: `feature/*` یا `fix/*` → `sprint/N`
- همیشه از طریق **Pull Request**، حتی اگه فقط خودت ریویو کنی.
- روش merge: **Squash and merge** (تاریخچه‌ی تمیز، یک commit به‌ازای هر تسک).
- بلافاصله بعد از merge، برنچ (لوکال و ریموت) حذف بشه.

### سطح ۲: `sprint/N` → `develop`
- فقط وقتی **همه‌ی** تسک‌های اون اسپرینت merge و تأیید شدن — نه زودتر، نه تسک‌به‌تسک.
- از طریق PR جداگانه.

### سطح ۳: `develop` → `main`
- فقط در نقاط عطف رسمی از پیش توافق‌شده (نه به‌صورت خودجوش توسط یک نفر).
- هرگز push مستقیم — حتی برای تغییرات "خیلی جزئی".

❌ لغزشی که قبلاً افتاد: یک commit (`update folders for accessibility`) مستقیماً و بدون PR روی `main` push شده بود. این نباید دوباره تکرار بشه — به همین دلیل قانون بعدی (بخش ۶) الزامیه.

---

## ۶. قوانین محافظتی الزامی (Branch Protection)

روی گیت‌هاب، برای `main` (و ترجیحاً `develop`) فعال بشه:
- ممنوعیت push مستقیم برای همه (حتی مالک ریپو).
- الزام باز بودن Pull Request قبل از هر merge.

این یه تنظیم فنیه که جلوی تکرار لغزش بخش ۵ رو فیزیکاً می‌گیره — نه فقط یه توافق شفاهی.

---

## ۷. پاکسازی برنچ‌ها

- برنچ‌های `feature/*` و `fix/*`: بلافاصله بعد از merge به sprint، هم لوکال و هم روی گیت‌هاب حذف بشن.
- برنچ‌های `sprint/*`: فقط بعد از merge کامل و تأییدشده به `develop` حذف بشن.
- قبل از حذف لوکال، `git branch -d` (نه `-D`) استفاده بشه — این دستور اگه برنچ واقعاً merge نشده باشه، خودش جلوی حذف اشتباهی رو می‌گیره.

---

## ۸. هماهنگی بین اعضای تیم (چند نفر / چند سیستم)

- هر جلسه‌ی کاری رو با `git fetch --all` شروع کنید تا از آخرین وضعیت هم‌تیمی خبردار بشید.
- هیچ‌وقت دو نفر همزمان روی یک برنچ مشترک push نکنید بدون هماهنگی مستقیم.
- کار نصفه‌کاره رو هم زود push کنید (روی برنچ خودتون) تا هم‌تیمی از روند کارتون خبر داشته باشه.
- قبل از ترک هر سیستم (وقتی روی چند سیستم کار می‌کنید): یا commit+push کنید، یا اگه آماده‌ی commit نیست، `git stash`.

---

## ۹. قانون نوشتن `.gitignore`

الگوها باید با `/` در ابتدا **anchor** بشن تا فقط ریشه‌ی پروژه رو مچ کنن، نه هر پوشه‌ای با همون اسم در هر عمقی.

❌ اشتباهی که قبلاً رخ داد: خط `models/` (بدون `/` اول) باعث شد `src/models/` و `tests/models/` (که کد واقعی پروژه بودن) هم به‌اشتباه ignore بشن.

✅ فرم درست: `/models/` — فقط پوشه‌ی `models` در ریشه رو ignore می‌کنه.

---

## ۱۰. چک‌لیست سریع قبل از باز کردن هر Pull Request

- [ ] اسم برنچ طبق قرارداد بخش ۳ هست؟
- [ ] پیام‌های commit طبق قرارداد بخش ۴ هستن؟
- [ ] مقصد PR درسته؟ (`feature`/`fix` → `sprint/N` خودش، نه مستقیم `main` یا `develop`)
- [ ] دیف رو حداقل یک‌بار خودتون مرور کردید؟
- [ ] برنچ `sprint/N` مقصد از قبل ساخته شده (طبق قانون بخش ۲)؟
